# API 智能测试平台 — 应用实践分享

---

## 目录

1. API自动化测试的痛点
2. API智能测试平台的价值
3. 技术架构
4. 主要功能介绍
5. 应用实践
6. 成效

---

## 一、API自动化测试的痛点

### 1.1 接口信息分散，管理混乱

- API 文档散落在 Swagger、Postman、Wiki、聊天记录等各处，缺乏统一管理和维护
- 前后端联调时频繁出现"接口找不到"、"文档过时"的问题
- 不同项目、不同团队的接口资产无法有效沉淀和复用

### 1.2 用例编写效率低，质量参差不齐

- 手工编写测试用例耗时长，一个完整的 API 测试场景往往需要数小时
- 边界场景、异常路径容易遗漏，测试覆盖率难以保证
- 不同测试人员编写的用例风格不统一，维护困难

### 1.3 测试维护成本高

- 接口变更（参数、路径、响应结构）导致大量已有用例需要同步修改
- 缺乏接口变更的自动感知机制，往往在执行失败后才被动发现
- 回归测试成本随业务规模增长而急剧上升

### 1.4 缺乏智能化能力

- 传统工具（Postman、JMeter）停留在"录制-回放"阶段，无法自动分析业务逻辑
- 无法根据 API 定义智能推荐测试策略和断言规则
- 测试设计高度依赖人工经验，新人上手周期长

### 1.5 团队协作困难

- 测试用例、环境配置、执行结果分散在个人电脑上
- 团队成员之间的测试资产难以共享和复用
- 缺乏统一的权限管理和操作审计

### 1.6 执行监控不足

- 缺乏实时的测试执行状态跟踪，无法及时发现执行异常
- 测试结果需要人工汇总，生成报告周期长
- 缺少历史趋势分析，无法量化质量度量指标

---

## 二、API智能测试平台的价值

### 2.1 AI 驱动，智能生成

- 接入大语言模型（OpenAI / DeepSeek / Claude / 通义千问 / 智谱 / Ollama 等），通过自然语言描述即可生成测试用例
- AI 自动分析 API 定义，智能推荐测试策略、断言规则和边界场景
- 基于 Function Calling 机制，AI 可自主搜索 API 库、获取接口详情、编排测试步骤

### 2.2 全流程覆盖，完整闭环

- 覆盖 API 采集 → 入库管理 → 用例生成 → 流程编排 → 套件执行 → 报告分析的全生命周期
- 打通"接口资产"与"测试资产"之间的壁垒，实现一体化管理
- 从手工操作到自动化的全面转型

### 2.3 可视化编排，降低门槛

- 基于 React Flow 的拖拽式流程画布，所见即所得的用例编排体验
- 多种节点类型（API 调用、等待、断言、并行分支）灵活组合
- 不需要编写代码，业务测试人员也能快速上手

### 2.4 统一管理，规范化资产

- 四层分类体系（平台 → 组件 → 功能 → 子功能），系统化管理 API 资产
- 标签、搜索、过滤等多维度管理能力
- 支持 HAR 导入、浏览器录制等多种方式快速沉淀接口资产

### 2.5 高效协作，团队赋能

- 多用户、多角色权限管理（管理员 / 普通用户）
- 测试用例、套件、报告等资产全团队共享复用
- 国际化支持（中文 / 英文），适配全球化团队

### 2.6 实时监控，全程可观测

- SSE（Server-Sent Events）实时推送执行日志，逐步骤展示执行详情
- 支持执行过程中的启动、停止、重试等控制操作
- Dashboard 仪表盘实时展示关键质量指标

---

## 三、技术架构

### 3.1 整体架构（五层）

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端展示层                                │
│   Next.js 16 + React 19 + Tailwind CSS + Radix UI + React Flow │
├─────────────────────────────────────────────────────────────────┤
│                        业务逻辑层                                │
│   Next.js API Routes + next-intl + TanStack Query + Zod        │
├─────────────────────────────────────────────────────────────────┤
│                        AI 引擎层                                 │
│   OpenAI SDK + Function Calling + SSE 流式输出                   │
│   支持: OpenAI / DeepSeek / Claude / 百度 / 阿里 / 智谱 / Ollama │
├─────────────────────────────────────────────────────────────────┤
│                        测试执行层                                │
│   Python FastAPI 执行引擎 + 变量管理 + 定时调度器                  │
├─────────────────────────────────────────────────────────────────┤
│                        数据存储层                                │
│   SQLite + Prisma ORM（15+ 数据模型）                            │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 前端技术栈

| 技术 | 用途 |
|------|------|
| Next.js 16 | 全栈框架，SSR + API Routes |
| React 19 | UI 组件库 |
| Tailwind CSS | 原子化 CSS 样式 |
| Radix UI | 无障碍 UI 组件 |
| React Flow (@xyflow/react) | 可视化流程编排画布 |
| TanStack React Query | 服务端状态管理 |
| react-hook-form + Zod | 表单管理与数据校验 |
| Recharts | 数据可视化图表 |
| next-intl | 国际化（中/英文） |

### 3.3 后端技术栈

| 技术 | 用途 |
|------|------|
| Next.js API Routes | RESTful API 接口 |
| Prisma ORM | 数据库访问层 |
| SQLite | 轻量级关系型数据库 |
| OpenAI SDK | 大模型调用（兼容多种模型） |
| SSE | 实时事件推送（执行日志、AI 生成） |

### 3.4 测试执行引擎

| 技术 | 用途 |
|------|------|
| Python FastAPI | 独立执行服务（端口 18014） |
| SuiteExecutor | 套件执行器 |
| TestExecutor | 单用例执行器 |
| VariableManager | 步骤间变量管理 |
| Scheduler | Cron 定时调度 |

### 3.5 数据模型概览

平台共设计 **15+ 数据模型**，覆盖完整业务域：

- **API 资产域**：Api、Classification（四层分类）、Tag、ApiTag
- **测试编排域**：TestCase、TestStep（流程配置 JSON）
- **套件执行域**：TestSuite、TestSuiteCase、TestSuiteExecution、TestCaseExecution、TestStepExecution、ExecutionLog
- **AI 会话域**：Conversation、ConversationMessage
- **平台配置域**：PlatformSettings（环境、认证、AI 配置）
- **用户权限域**：User、Session

---

## 四、主要功能介绍

### 4.1 API 采集 — 多模式接口录制

平台提供四种 API 采集方式，覆盖各种场景需求：

#### 浏览器录制
- 基于 Playwright Chromium 内核，启动浏览器实时捕获 API 请求
- 支持暂停 / 恢复 / 停止控制
- SSE 流式实时推送捕获到的请求数据

#### 代理录制
- HTTP 代理模式，配置代理地址后自动捕获经过代理的所有 API 请求
- 适用于需要在已有浏览器中操作的场景

#### Mitmproxy 录制
- 中间人代理方式，支持 HTTPS 解密和录制
- 适用于移动端 APP 接口测试场景

#### HAR 文件导入
- 支持从浏览器开发者工具导出的 HAR 文件批量导入
- 自动解析 HAR 条目，智能识别路径参数（如 `/api/users/123` → `/api/users/{id}`）
- 支持 Header 过滤配置，仅保留关键请求头

#### 采集核心能力
- **智能去重检测**：自动检测已存在的 API，避免重复录入
- **批量保存**：一键将采集到的 API 批量保存至 API 仓库
- **冲突解决**：遇到重复 API 时支持跳过、覆盖、重命名等策略
- **分类选择**：保存时可选择四层分类归属

### 4.2 API 仓库 — 四层分类管理体系

#### 四层分类架构

```
L1 - 平台 (Platform)      → 业务系统级别划分（如：电商平台、CRM系统）
  └─ L2 - 组件 (Component)  → 服务/模块级别划分（如：用户服务、订单服务）
      └─ L3 - 功能 (Feature)  → 功能模块级别划分（如：用户注册、订单创建）
          └─ L4 - 子功能 (SubFeature) → API 级别精细分类
```

#### 核心能力
- **树形浏览导航**：直观的分类树结构，支持展开/折叠，快速定位目标 API
- **完整 API 信息管理**：Method、URL、Headers、Query、Body、Response、Schema 全面覆盖
- **标签管理**：灵活的标签分类，支持多标签筛选
- **多维度搜索**：按名称、URL、方法、分类等多维度搜索过滤
- **批量操作**：批量编辑、分类调整、删除等操作
- **分类管理**：支持创建、编辑、删除分类节点

### 4.3 AI 智能生成 — 对话式用例生成

#### 核心流程

```
用户描述测试需求（自然语言）
        ↓
AI 通过 Function Calling 搜索 API 库
        ↓
获取 API 详情并分析测试策略
        ↓
自动编排测试步骤、断言规则
        ↓
生成可执行的测试用例（保存到数据库）
```

#### AI 工具链（Function Calling）

| 工具函数 | 功能 |
|---------|------|
| `hierarchical_search_apis` | 按四层分类搜索 API（平台/组件/功能/API名称） |
| `get_api_detail` | 获取单个 API 的完整详情 |
| `smart_search_delete_api` | 查找清理类 API（用于测试后数据清理） |
| `assemble_and_create_test_cases` | 自动编排并创建测试用例 |

#### 多模型支持

| 模型厂商 | 支持模型 |
|---------|---------|
| OpenAI | GPT-4o / GPT-4 / GPT-3.5 等 |
| DeepSeek | DeepSeek-V3 / DeepSeek-R1 等 |
| Anthropic | Claude 3.5 / Claude 3 等 |
| 百度文心 | 文心一言系列 |
| 阿里通义 | 通义千问系列 |
| 智谱 AI | GLM 系列 |
| Ollama | 本地部署的开源模型 |

#### 交互体验
- **SSE 流式输出**：实时展示 AI 的思考过程、工具调用、生成结果
- **对话式会话管理**：支持多轮对话、上下文记忆
- **历史会话**：会话列表管理，随时回顾历史生成记录
- **思考展示**：展示 AI 的思考链（Thinking Block），增加透明度

### 4.4 用例编排 — 可视化流程设计

#### 基于 React Flow 的可视化画布

通过拖拽式操作将不同类型的节点组合为完整的测试流程，所见即所得。

#### 节点类型

| 节点 | 功能 | 说明 |
|------|------|------|
| Start 节点 | 流程起始 | 每个用例的入口点 |
| API 节点 | 调用 API | 配置请求方法、URL、参数、Header、Body |
| Wait 节点 | 等待延时 | 等待指定时间后继续执行 |
| Assertion 节点 | 断言验证 | 对响应结果进行断言检查 |
| Parallel 节点 | 并行分支 | 并行执行多个步骤 |
| End 节点 | 流程结束 | 用例执行终止点 |

#### 编排能力
- **参数化配置**：Headers / Query / Path / Body 参数灵活配置
- **变量提取与传递**：从响应中提取变量（JSONPath），供后续步骤引用
- **断言验证**：状态码、响应体、Header、JSONPath 等多种断言方式
- **优先级管理**：P0 - P3 四级优先级，灵活管理用例执行策略
- **API 选择器**：从 API 仓库中快速选择接口，自动填充请求信息
- **流程连线**：节点间通过连线定义执行顺序和依赖关系

### 4.5 测试套件与执行监控

#### 测试套件管理
- **套件组织**：将多个测试用例组织为可执行的测试套件
- **用例排序**：拖拽调整用例执行顺序
- **启用/禁用**：灵活控制套件中每个用例是否参与执行
- **环境配置**：每个套件独立配置 BaseURL、认证 Token、Session Cookie、自定义 Header

#### 定时调度
- 支持 Cron 表达式配置定时执行计划
- 实现无人值守的持续回归测试
- 例如：每日凌晨 2:00 自动执行核心业务回归套件

#### 执行监控
- **SSE 实时日志流**：逐条推送步骤执行详情（请求、响应、断言结果）
- **执行控制**：支持启动 / 停止 / 重试操作
- **三级执行记录**：套件执行 → 用例执行 → 步骤执行，层层追溯
- **Python 执行引擎**：FastAPI 独立服务（端口 18014），包含变量管理器和定时调度器

### 4.6 测试报告与数据分析

#### Dashboard 仪表盘
- API 总数、测试用例数、测试套件数等关键统计
- 最近执行记录和通过率概览
- 可视化图表展示趋势变化（基于 Recharts）

#### 报告能力
- **执行历史追溯**：完整记录每次执行的开始时间、结束时间、执行状态、通过率
- **详细步骤日志**：每个步骤的请求报文、响应报文、断言结果逐一展示
- **成功率分析**：测试通过率趋势图，直观反映质量变化
- **多维度筛选**：按套件、时间范围、执行状态、用例名称灵活筛选
- **执行日志控制台**：类终端风格的日志展示，支持级别过滤

---

## 五、应用实践

### 5.1 端到端测试流程

```
API采集 → 入库管理 → AI生成用例 → 可视化编排 → 套件执行 → 测试报告
```

整个流程形成完整闭环，从接口发现到质量度量全链路覆盖。

### 5.2 场景一：核心业务接口回归测试

**背景**：系统迭代频繁，每次发版需要验证核心接口是否正常

**实践步骤**：
1. 通过 HAR 导入，批量采集核心业务模块的所有 API 接口
2. 在 API 仓库中按四层分类体系规范化管理（如：电商平台 → 订单服务 → 订单管理 → CRUD）
3. 使用 AI 生成功能，用自然语言描述："为订单创建接口生成完整的测试用例，包括正常创建、参数缺失、权限验证等场景"
4. 在可视化画布中微调用例流程，补充变量传递和数据清理步骤
5. 创建回归测试套件，配置测试环境和认证信息
6. 设置 Cron 定时任务，每日凌晨自动执行
7. 通过 Dashboard 和执行报告查看每日回归结果

**效果**：每日自动完成 200+ 核心接口的回归验证，第一时间发现接口异常。

### 5.3 场景二：新功能 API 联调验证

**背景**：新功能开发完成后，需要快速验证新增 API 的正确性

**实践步骤**：
1. 前端开发联调时，使用浏览器录制模式实时捕获新增 API 请求
2. 平台自动去重检测，仅保存新增的接口到 API 仓库
3. AI 分析新接口的参数定义，自动生成正向 + 异常场景测试用例
4. 在可视化画布中编排完整的业务流程（如：创建 → 查询 → 修改 → 删除）
5. 立即执行验证，实时查看每步执行结果和断言情况

**效果**：新接口从录制到完成测试仅需 30 分钟，传统方式需要半天。

### 5.4 场景三：跨系统集成测试

**背景**：多个子系统之间的接口交互需要端到端验证

**实践步骤**：
1. 利用四层分类体系管理多系统 API 资产（L1 区分不同平台/系统）
2. 在用例编排中跨系统选择 API，利用并行节点模拟并发调用
3. 通过变量提取和传递，将系统 A 的响应作为系统 B 的输入参数
4. 配置套件环境时，设置不同系统的 BaseURL 和认证信息
5. 使用断言节点验证跨系统数据一致性

**效果**：集成测试用例维护成本降低 50%，端到端场景覆盖率提升 40%。

### 5.5 最佳实践总结

| 实践要点 | 说明 |
|---------|------|
| API 资产先行 | 先通过采集和导入建立完整的 API 资产库，为后续测试奠定基础 |
| 分类规范化 | 严格按照四层分类体系组织 API，便于 AI 搜索和团队复用 |
| AI 生成 + 人工微调 | AI 生成初始用例，人工补充业务特定的断言和数据准备 |
| 定时回归常态化 | 核心业务套件配置定时执行，实现持续质量监控 |
| 变量驱动编排 | 充分利用变量提取和传递，实现步骤间的数据联动 |

---

## 六、成效

### 6.1 关键指标提升

| 指标 | 提升幅度 | 说明 |
|------|---------|------|
| 用例编写效率 | **提升 70%+** | AI 辅助生成 + 可视化编排，大幅减少手工编写时间 |
| 回归执行时间 | **缩短 60%+** | 自动化替代手工执行，定时无人值守 |
| 接口覆盖率 | **达到 95%+** | 系统化管理 + AI 补充边界场景 |
| 缺陷发现率 | **提升 40%+** | AI 智能补充边界用例，减少人工遗漏 |

### 6.2 质量提升

- **API 资产统一管理**：消除信息孤岛，全团队共享接口资产，避免重复录入和维护
- **AI 智能补充边界用例**：大模型自动分析参数约束，生成人工容易遗漏的边界场景和异常测试
- **可视化编排降低门槛**：业务测试人员无需编程能力即可编排复杂测试流程，全员可参与自动化
- **测试规范化**：四层分类体系 + 优先级管理，确保测试工作有章可循

### 6.3 效率提升

- **定时回归执行**：核心业务套件每日自动执行，无人值守持续验证，问题发现前移
- **全流程闭环提效**：从 API 采集到测试报告的一站式平台，消除工具切换和数据搬运的成本
- **团队协作赋能**：测试用例、套件、报告全团队可见可用，消除重复劳动
- **新人快速上手**：可视化操作 + AI 辅助，新成员培训周期从 2 周缩短至 3 天

### 6.4 技术亮点总结

| 亮点 | 说明 |
|------|------|
| 多模型适配 | 支持 7+ 大模型厂商，可根据成本和效果灵活切换 |
| Function Calling | AI 自主调用工具链，实现从搜索到生成的全自动流程 |
| SSE 实时流 | 执行日志和 AI 生成过程实时可观测 |
| 可视化编排 | React Flow 拖拽式画布，6 种节点类型灵活组合 |
| 四层分类 | 系统化 API 资产管理，支撑 AI 精准搜索 |
| 独立执行引擎 | Python FastAPI 执行服务，解耦前端与执行逻辑 |
| 国际化 | 中英文双语支持，适配全球化团队 |

---

> **API 智能测试平台**通过 AI 驱动 + 全流程覆盖 + 可视化编排的方式，将 API 测试从"人工密集型"转变为"智能辅助型"，显著提升了测试效率和质量。

---

*感谢聆听 · Q&A*
