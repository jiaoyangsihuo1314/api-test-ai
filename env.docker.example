# ================================
# API 智能测试平台 - Docker 部署环境变量配置示例
# 使用方法：复制此文件为 .env 并根据实际情况修改
# ================================

# 应用环境
NODE_ENV=production

# 数据库配置
# All-in-One 模式：使用容器内路径
DATABASE_URL=file:/app/prisma/dev.db

# 执行器地址配置
# All-in-One 模式：前端和执行器在同一容器，使用 localhost
EXECUTOR_URL=http://localhost:8001

# 如果使用独立部署（frontend + executor 分开），修改为：
# EXECUTOR_URL=http://executor:8001

# API 配置（可选）
# OPENAI_API_KEY=your-openai-api-key
# OPENAI_BASE_URL=https://api.openai.com/v1

# DeepSeek API（可选）
# DEEPSEEK_API_KEY=your-deepseek-api-key
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# 其他 AI 提供商（可选）
# ANTHROPIC_API_KEY=your-anthropic-api-key

# 容器配置
DOCKER_REGISTRY=docker.io
DOCKER_USERNAME=yourname
VERSION=latest

